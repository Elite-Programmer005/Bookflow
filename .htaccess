RewriteEngine On
RewriteBase /

# Keep real files and directories accessible.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route all clean URLs to the front controller.
RewriteRule ^ index.php [QSA,L]
